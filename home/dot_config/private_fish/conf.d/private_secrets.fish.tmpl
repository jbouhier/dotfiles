# Gemini
# NOTE: fish `set` does not use `=`; `set -gx VAR = value` sets a two-element
# list ["=", value]. Pass the value positionally as a single quoted argument.
set -gx GEMINI_API_KEY "{{ keyring "google" "gemini" }}"
set -gx GEMINI_MODEL "gemini-3-pro-preview"
